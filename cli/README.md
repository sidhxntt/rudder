# Rudder CLI

The supported CLI is the Node/TypeScript package in [`node/`](node/). It is a
client of the same Rudder control-plane API used by the web console.

```bash
cd cli/node
npm install
npm link
rudder help
```

The old Python CLI and generated Python SDK were retired in Phase 9.
