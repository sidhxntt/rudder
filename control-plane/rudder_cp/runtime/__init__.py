"""Runtime adapters.

Rudder owns product intent.  A runtime adapter owns the translation of that
intent to actual workload resources.  Docker remains the default until the
Kubernetes runtime is explicitly selected.
"""
